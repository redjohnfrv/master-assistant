import React from 'react'import {RenderVariant} from '../../types'type Props = {  onClick: (variant: RenderVariant) => void}export const List = (props: Props) => {  const {onClick} = props  return (    <ul>      <li onClick={() => onClick('list')}>Set Timer</li>      <li onClick={() => onClick('timer')}>Dice Roll</li>      <li>Notion list</li>    </ul>  )}