import React, {BaseSyntheticEvent} from 'react'import styled from 'styled-components'import {UseFormRegister} from 'react-hook-form'import {TimerFormType} from '../../types'import {Button, Input} from '../../ui'type Props = {  register: UseFormRegister<TimerFormType>  onTimerSubmit: (e?: BaseSyntheticEvent | undefined) => Promise<void>}export const SetTimer = (props: Props) => {  const {register, onTimerSubmit} = props  return (    <Root>      <form onSubmit={onTimerSubmit}>        <StyledInput type="text" maxLength={5} {...register('timestamp')} />        <StyledButton type="submit" name="&#8730;" />      </form>    </Root>  )}const Root = styled.div`  & form {    width: 100%;    display: flex;    justify-content: center;    gap: 28px;  }`const StyledInput = styled(Input)`  width: 92px;`const StyledButton = styled(Button)`  width: 35px;  color: #FFF;  border: 2px solid #FFF;  border-radius: 8px;  background: ${({theme: {colors}}) => colors.primary[300]};    &:hover {    background: ${({theme: {colors}}) => colors.primary[200]};  }  &:active {    background: ${({theme: {colors}}) => colors.primary[400]};  }`