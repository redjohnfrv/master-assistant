import React, {BaseSyntheticEvent} from 'react'import styled from 'styled-components'import {UseFormRegister} from 'react-hook-form'import {TimerFormType} from '../../types'import {Button, Input} from '../../ui'type Props = {  register: UseFormRegister<TimerFormType>  onTimerSubmit: (e?: BaseSyntheticEvent | undefined) => Promise<void>}export const SetTimer = (props: Props) => {  const {register, onTimerSubmit} = props  return (    <Root>      <form onSubmit={onTimerSubmit}>        <Input {...register('timestamp')} />        <Button type="submit" name="Ok" />      </form>    </Root>  )}const Root = styled.div`  & form {    display: flex;    flex-direction: column;  }`