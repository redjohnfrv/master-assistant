import React from 'react'import styled from 'styled-components'import {useTimer} from 'react-timer-hook'import {getDoubleZeroTime} from '../../utils/fromNumber/getDoubleZeroTime'type Props = {  expiryTimestamp: number}export const Timer = (props: Props) => {  const {expiryTimestamp} = props  const time = new Date()  time.setSeconds(time.getSeconds() + expiryTimestamp)  const {    seconds,    minutes,    hours,    start,    pause,    restart,  } = useTimer({expiryTimestamp: time})  return (    <Root>      <div style={{textAlign: 'center'}}>        <div style={{fontSize: '15px'}}>          {getDoubleZeroTime(hours)}:{getDoubleZeroTime(minutes)}:{getDoubleZeroTime(seconds)}        </div>        <button onClick={start}>Start</button>        <button onClick={pause}>Pause</button>        <button onClick={() => {          restart(time)        }}>Restart        </button>      </div>    </Root>  )}const Root = styled.section`  width: 100%;`