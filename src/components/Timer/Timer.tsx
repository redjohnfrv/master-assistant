import React, {useEffect} from 'react'import styled from 'styled-components'import {useTimer} from 'react-timer-hook'import {fromNumber} from '../../utils/fromNumber'type Props = {  expiryTimestamp: number}export const Timer = (props: Props) => {  const {expiryTimestamp} = props  const time = new Date()  time.setSeconds(time.getSeconds() + expiryTimestamp)  const {    seconds,    minutes,    hours,    start,    pause,    restart,  } = useTimer({expiryTimestamp: time})  useEffect(() => {    restart(time)    // eslint-disable-next-line react-hooks/exhaustive-deps  }, [expiryTimestamp])  return (    <Root>      <div style={{textAlign: 'center'}}>        <div style={{fontSize: '15px'}}>          {fromNumber.getDoubleZeroTime(hours)}:          {fromNumber.getDoubleZeroTime(minutes)}:          {fromNumber.getDoubleZeroTime(seconds)}        </div>        <button onClick={start}>Start</button>        <button onClick={pause}>Pause</button>        <button onClick={() => {          restart(time)        }}>Restart        </button>      </div>    </Root>  )}const Root = styled.section`  width: 100%;`