import React, {ReactNode} from 'react'import styled from 'styled-components'import {Timer, Watch} from '../../components'import {BackgroundImage, TopLine} from './components'type Props = {  children: ReactNode  timestamp: number}export const Layout = (props: Props) => {  const {children, timestamp} = props  return (    <Root>      <BackgroundImage />      <TopLine>        <Watch />        <Timer expiryTimestamp={timestamp} />      </TopLine>      {children}    </Root>  )}const Root = styled.div`  position: relative;  display: flex;  flex-direction: column;  max-width: 667px;  height: 100vh;  margin: 0 auto;  padding: 12px;`