import React, {ReactNode} from 'react'import styled from 'styled-components'import { RenderVariant } from '../../../types'type Props = {  children: ReactNode  onSetRenderElement: (variant: RenderVariant) => void}export const ContentLayout = (props: Props) => {  const {children, onSetRenderElement} = props  return (    <Root>      {children}      <button onClick={() => onSetRenderElement('list')}>Back to list</button>    </Root>  )}const Root = styled.div`  width: 100%;  height: 100%;    &>button {    position: absolute;    bottom: 24px;    left: 50%;    transform: translateX(-50%);  }`